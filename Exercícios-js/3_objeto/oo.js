// CODIGO NÃO EXECUTÁVEL

// PROCEDURAL
processamento(valor1, valor2, valor3)

//OO
 objeto = {
    valor1, 
    valor2, 
    valor3, 
    processamento() {
        // ...
    }
 }

 objeto.processamento() // Foco passou a ser o objeto
 /* 
 * 1. Abstração -> saber trazer o problema ao programa de forma objetiva
 * 2. Encapsulamento -> manter a comunicação entre os objetos simples e objetiva, promover a independência
 * 3. Herança (prototype)
 * 4. Polimorfismo
 */